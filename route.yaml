kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: imageidentificationroute
  namespace: demoopenshift
spec:
  host: imageidentificationroute-demoopenshift.apps.hpeopenshift.eastus.aroapp.io
  to:
    kind: Service
    name: reactservice
  port:
    targetPort: 3000
