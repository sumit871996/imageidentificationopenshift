apiVersion: v1
kind: Service
metadata:
  name: reactservice
  namespace: imageadentificationapplication
spec:
  selector:
    app: reactapp
  ports:
    - targetPort: 3000
      protocol: TCP
      port: 3000
