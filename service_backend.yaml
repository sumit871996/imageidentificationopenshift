apiVersion: v1
kind: Service
metadata:
  name: fastapiservice
  namespace: imageidentification
spec:
  selector:
    app: fastapiapp
  
  ports:
    - targetPort: 8000
      protocol: TCP
      port: 8000   
